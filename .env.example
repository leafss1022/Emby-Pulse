# Emby Stats 环境变量配置示例
# 复制此文件为 .env 并修改配置

# 时区
TZ=Asia/Shanghai

# Emby 服务器地址
EMBY_URL=http://localhost:8096

# Emby API Key（可选，不填则自动从数据库获取）
# 在 Emby 控制台 -> 高级 -> API 密钥 中创建
EMBY_API_KEY=

# 数据库路径（容器内路径，对应挂载的 Emby 数据目录）
PLAYBACK_DB=/data/playback_reporting.db
USERS_DB=/data/users.db
AUTH_DB=/data/authentication.db

# 最小播放时长过滤（秒），低于此时长的记录不计入播放次数（时长仍统计）
# 设为 0 或留空表示不过滤，例如设为 300 表示 5 分钟以下不计次数
MIN_PLAY_DURATION=0

# 时区偏移（小时），用于 SQLite 查询时间转换
# 上海/北京为 +8，东京为 +9，纽约为 -5
TZ_OFFSET=8
